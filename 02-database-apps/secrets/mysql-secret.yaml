apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  labels:
    app: mysql-app
type: Opaque
data:
  # The password is base64 encoded for security
  # Current password: "mySecurePassword"
  # To use your own password:
  #   1. Run: echo -n 'YourPassword' | base64
  #   2. Replace the value below with the output
  mysql-root-password: bXlTZWN1cmVQYXNzd29yZA==
